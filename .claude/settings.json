{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'source ~/.bashrc && conda activate food-opt && ruff check --fix . && ruff format .'"
          }
        ]
      }
    ]
  }
}